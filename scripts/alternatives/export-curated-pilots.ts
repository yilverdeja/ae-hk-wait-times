/**
 * One-time export of hand-curated pilot entries into `data/alternatives/entries/`.
 * Completed during the data folder refactor — pilot entries already live on disk.
 */
console.log(
    "export-curated-pilots: already run. Edit data/alternatives/entries/*.ts directly.",
)
process.exit(0)
